"use client"
import { Card } from "@/components/ui/card"
import { Button } from "@/components/ui/button"
import { TrendingUp, TrendingDown, PiggyBank, Plus, BarChart3, MapPin } from "lucide-react"
import type { Transaction, CATEGORIES } from "@/app/page"
import { InflationTracker } from "@/components/inflation-tracker"

interface DashboardProps {
  transactions: Transaction[]
  onAddTransaction: () => void
  onViewAnalytics: () => void
  onViewTransactions: () => void
  onViewPriceMaps: () => void
}

export function Dashboard({ transactions, onAddTransaction, onViewAnalytics, onViewTransactions, onViewPriceMaps }: DashboardProps) {
  // Use your earlier dashboard.tsx implementation exactly
}
"use client"
import { Card } from "@/components/ui/card"
import { Button } from "@/components/ui/button"
import { TrendingUp, TrendingDown, PiggyBank, Plus, BarChart3, MapPin } from "lucide-react"
import type { Transaction, CATEGORIES } from "@/app/page"
import { InflationTracker } from "@/components/inflation-tracker"

interface DashboardProps {
  transactions: Transaction[]
  onAddTransaction: () => void
  onViewAnalytics: () => void
  onViewTransactions: () => void
  onViewPriceMaps: () => void
}

export function Dashboard({ transactions, onAddTransaction, onViewAnalytics, onViewTransactions, onViewPriceMaps }: DashboardProps) {
  // Use your earlier dashboard.tsx implementation exactly
}
"use client"
import { Card } from "@/components/ui/card"
import { Button } from "@/components/ui/button"
import { TrendingUp, TrendingDown, PiggyBank, Plus, BarChart3, MapPin } from "lucide-react"
import type { Transaction, CATEGORIES } from "@/app/page"
import { InflationTracker } from "@/components/inflation-tracker"

interface DashboardProps {
  transactions: Transaction[]
  onAddTransaction: () => void
  onViewAnalytics: () => void
  onViewTransactions: () => void
  onViewPriceMaps: () => void
}

export function Dashboard({ transactions, onAddTransaction, onViewAnalytics, onViewTransactions, onViewPriceMaps }: DashboardProps) {
  // Use your earlier dashboard.tsx implementation exactly
}
